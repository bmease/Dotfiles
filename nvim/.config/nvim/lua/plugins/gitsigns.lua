require('gitsigns').setup({
  signs = {
      -- add = { text='' },
      -- change = { text='' },
      -- delete = { text='' }
      add = { text='▌' },
      change = { text='▌' },
      delete = { text='▌' }
  }
})
